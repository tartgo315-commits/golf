{
  "buildCommand": "npm run build",
  "installCommand": "npm install",
  "outputDirectory": "dist",
  "framework": null,
  "redirects": [{ "source": "/favicon.ico", "destination": "/favicon.png" }],
  "rewrites": [{ "source": "/(.*)", "destination": "/" }]
}
